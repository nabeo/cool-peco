##
# select docker container id by docker ps -a
#
local res
res=$(docker ps -a | egrep -v '^CONTAINER ID' | peco --query "$LBUFFER" | awk '{ print $1}')
if [ -n $res ]; then
  _cool-peco-insert-command-line " $res"
fi
